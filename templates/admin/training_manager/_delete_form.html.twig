<form method="post" action="{{ path('admin_training_manager_delete', {'id': training_manager.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_manager.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
