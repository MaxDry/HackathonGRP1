<form method="post" action="{{ path('admin_training_measure_delete', {'id': training_measure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_measure.id) }}">
    <button class="btn">Delete</button>
</form>
